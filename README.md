# 🏆 Gold Analyzer

یک برنامه خودکار برای تحلیل تکنیکال و تولید سیگنال معاملاتی برای طلا (Gold Futures).

## 📋 ویژگی‌ها

- 🔄 بررسی خودکار هر یک دقیقه
- 📊 محاسبه اندیکاتورهای تکنیکال:
  - **RSI** (Relative Strength Index) - برای شناسایی اشباع خریدار/فروش‌فروش
  - **MACD** (Moving Average Convergence Divergence) - برای شناسایی روند
  - **ATR** (Average True Range) - برای اندازه‌گیری نوسانات
  
- 🎯 تولید سیگنال‌های معاملاتی:
  - ✅ **BUY** (خریدش کن)
  - ❌ **SELL** (بفروش)
  - ⏸️ **HOLD** (نگاه کن)

- 📈 تولید نمودارهای تعاملی (Interactive Charts):
  - نمودار قیمت با نمایش سیگنال‌های خرید/فروش
  - نمودار RSI با نشانگر حد‌های اشباع
  - نمودار MACD با خطوط سیگنال و هیستوگرام
  - داشبورد HTML تعاملی برای مشاهده همه نمودارها

- 📝 logging خودکار به فایل (فقط سیگنال‌های BUY و SELL)
- ⚙️ تنظیمات قابل سفارشی‌سازی

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- Go 1.25.2 یا بالاتر
- اتصال اینترنت (برای دریافت داده‌های Yahoo Finance)

### نصب

```bash
git clone <repository-url>
cd gold-analyzer
go mod download
```

### اجرا

```bash
go run ./cmd/main.go
```

یا برای اجرا به صورت binary:

```bash
go build -o analyzer ./cmd/main.go
./analyzer
```

### اجرا با تولید نمودارها

```bash
make run-with-charts
```

یا به صورت دستی:

```bash
CHART_OUTPUT_DIR="./charts" ./analyzer
```

نمودارها در پوشهٔ `./charts/` تولید شده و داشبورد اصلی در `./charts/dashboard.html` قابل دسترسی است.

## 📊 ساختار پروژه

```
gold-analyzer/
├── cmd/
│   └── main.go              # نقطه ورود برنامه
├── config/
│   └── config.go            # تنظیمات قابل سفارشی
├── chart/
│   ├── chart.go            # تولید نمودارها
│   └── dashboard.go        # تولید داشبورد HTML
├── indicators/
│   ├── rsi.go              # محاسبه RSI
│   ├── macd.go             # محاسبه MACD
│   └── atr.go              # محاسبه ATR
├── model/
│   └── candle.go           # ساختار داده شمع
├── strategy/
│   └── gold_strategy.go    # منطق تصمیم‌گیری
├── yahoo/
│   └── client.go           # کلاینت Yahoo Finance
├── go.mod                  # فایل ماژول
└── README.md              # این فایل
```

## 🔧 تنظیمات

تنظیمات پیش‌فرض در فایل `config/config.go`:

```go
// نماد معاملاتی
Symbol: "GC=F"  // طلا (Futures)

// بازه زمانی داده‌ها
Interval: "1h"  // ساعتی

// محدوده داده‌های تاریخی
Range: "7d"     // 7 روز

// فاصله بررسی خودکار
CheckInterval: 1 * time.Minute

// پارامترهای اندیکاتورها
RSIPeriod: 14
ATRPeriod: 14

// حد‌های سیگنال
RSIBuyLower: 40      // حد پایین برای خرید
RSIBuyUpper: 55      // حد بالای برای خرید
RSISellThreshold: 65 // حد فروش

// Log file (فقط سیگنال‌های BUY و SELL)
LogFile: ""  // خالی = بدون logging

// Output directory for charts
ChartOutputDir: "./charts"  // پوشهٔ تولید نمودارها
```

برای تغییر تنظیمات، می‌توانید از متغیرهای محیطی استفاده کنید:

```bash
# مثال
CHART_OUTPUT_DIR="./my_charts" LOG_FILE="signals.log" ./analyzer
```

## 📈 سیگنال‌های معاملاتی

### ✅ سیگنال خرید (BUY)
شرایط:
- RSI بین 40 تا 55 باشد
- MACD Histogram مثبت باشد
- ATR صعودی باشد

### ❌ سیگنال فروش (SELL)
شرایط:
- RSI بالاتر از 65 باشد (اشباع خریدار)
- MACD Histogram منفی باشد

### ⏸️ سیگنال انتظار (HOLD)
- شرایط برای خرید یا فروش برقرار نیستند

## 🛑 Graceful Shutdown

برنامه از **graceful shutdown** پشتیبانی می‌کند:
- ✅ سیگنال‌های SIGINT (Ctrl+C) و SIGTERM را پردازش می‌کند
- ✅ عملیات جاری را تکمیل می‌کند
- ✅ منابع را به درستی بسته می‌کند
- ✅ داده‌ها را ذخیره می‌کند

### استفاده
```bash
# برنامه را اجرا کنید
./analyzer

# برای متوقف کردن، Ctrl+C را فشار دهید
# یا: kill -TERM <PID>

# تنظیم timeout برای shutdown
SHUTDOWN_TIMEOUT_SECONDS=10 ./analyzer
```

برای اطلاعات بیشتر: [GRACEFUL_SHUTDOWN.md](./GRACEFUL_SHUTDOWN.md)

## 📊 نمونهٔ خروجی

```
🚀 Gold Analyzer - شروع نظارت خودکار...
======================================================================
⚙️  تنظیمات:
   • نماد: GC=F
   • بازه زمانی: 1h
   • محدوده: 7d
   • فاصله بررسی: 1m0s
======================================================================

💡 برای متوقف کردن، Ctrl+C را فشار دهید...

📊 بررسی در: 2025-12-14 22:41:35
----------------------------------------------------------------------

💰 قیمت فعلی طلا: 4328.30 USD
   ↓ تغییر: -1.50 USD (-0.03%)

📈 اندیکاتورهای تکنیکال:
   • RSI (14):        52.91 🟩 محدوده مناسب
   • MACD:            4.358991
   • MACD Signal:     7.511768
   • MACD Histogram:  -3.152777 📉 منفی
   • ATR (14):        29.50

🎯 سیگنال معاملاتی:
   ⏸️  سیگنال: نگاه کن (HOLD)
   دلایل سیگنال انتظار:
      • شرایط بازار مناسب برای ورود نیست
      • RSI = 52.91, MACD Hist = -3.152777, ATR = 29.50
======================================================================
```

## 🔐 توجهات امنیتی

- برنامه از Yahoo Finance API استفاده می‌کند (رایگان و بدون نیاز به API key)
- داده‌های حساس ذخیره نشده‌اند
- برنامه فقط خواندن داده‌ها انجام می‌دهد، تعدیلی ندارد

## ⚠️ سلب مسئولیت

**این برنامه صرفاً برای تحلیل تکنیکال است و توصیه‌های ارائه شده:**
- تضمین شدهٔ نیستند
- ریسک تلفات مالی وجود دارد
- نباید به عنوان تنها منبع تصمیم‌گیری استفاده شود
- همیشه از متخصصان مالی مشورت بگیرید

## 📊 نمودارها (Charts)

برنامه سه نوع نمودار تعاملی تولید می‌کند:

### 📈 نمودار قیمت (Price Chart)
- نمایش قیمت طلا در طول زمان
- علامت‌گذاری سیگنال‌های خرید (🟢) و فروش (🔴)
- قابل‌ بزرگ‌نمایی و کاوش

### 📊 نمودار RSI
- نمایش مقادیر RSI
- خطوط حد اشباع خریدار (70) و فروش‌فروش (30)
- تشخیص شرایط افراط‌گرایانه

### 📉 نمودار MACD
- خط MACD و خط سیگنال
- هیستوگرام MACD
- تشخیص تغییرات روند

### 🎨 داشبورد HTML
- تمام نمودارها در یک صفحه
- رابط کاربری زیبا و واکنش‌پذیر
- قابل باز شدن در هر مرورگری

فایل اصلی: `./charts/dashboard.html`

## 📝 لاگ کردن (Logging)

برنامه **فقط سیگنال‌های BUY و SELL** را در فایل لاگ ذخیره می‌کند:

```bash
LOG_FILE="gold_signals.log" ./analyzer
```

فرمت لاگ:
```
[2025-12-14 22:41:35] Signal: BUY | Price: 4328.30 | RSI: 52.91 | MACD: 1.459146 | ATR: 29.50
[2025-12-14 23:41:35] Signal: SELL | Price: 4340.50 | RSI: 68.45 | MACD: -2.341000 | ATR: 31.20
```

سیگنال‌های HOLD در لاگ ذخیره نمی‌شوند.

## 🛠️ دستورات Makefile

```bash
# کامپایل و اجرا
make run                    # اجرا معمولی
make run-with-log          # اجرا با ذخیره لاگ
make run-with-charts       # اجرا با تولید نمودارها
make dev-charts            # پاک کردن، کامپایل و اجرا با نمودارها

# تست و بررسی کیفیت
make test                  # اجرای تست‌ها
make lint                  # بررسی کیفیت کد
make fmt                   # فرمت کردن کد

# دیگر دستورات
make clean                 # پاک کردن فایل‌های تولید شده
make help                  # نمایش تمام دستورات
```

## 🤝 مشارکت

برای بهبود برنامه:
1. Fork کنید
2. یک branch جدید بسازید
3. تغییرات را commit کنید
4. Pull request بسازید

## 📄 لایسنس

این پروژه تحت لایسنس MIT است.

## 📞 تماس

برای سؤالات و پیشنهادات، issue باز کنید.

---

**نسخه**: 1.0.0  
**آخرین بروزرسانی**: دسامبر 2025
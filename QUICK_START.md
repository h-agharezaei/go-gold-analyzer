# 🚀 شروع سریع Gold Analyzer

## 5 دقیقه برای شروع!

### 1️⃣ نصب و راه‌اندازی

```bash
# کلون کردن پروژه
git clone <repository-url>
cd gold-analyzer

# دانلود وابستگی‌ها
go mod download
```

### 2️⃣ اجرای برنامه

```bash
# اجرای ساده
go run ./cmd/main.go

# یا کامپایل و اجرا
make build
./analyzer
```

### 3️⃣ اجرا با Logging

```bash
# اجرا با ذخیره سیگنال‌ها در فایل
LOG_FILE="signals.log" go run ./cmd/main.go

# یا از Makefile
make run-with-log
```

### 4️⃣ تغییر تنظیمات

تنظیمات را می‌توانید از طریق environment variables تغییر دهید:

```bash
# نمادها و بازه‌های مختلف
SYMBOL=EURUSD=X INTERVAL=5m RANGE=1d go run ./cmd/main.go

# یا فایل .env رو بسازید
cp .env.example .env
# حالا فایل .env رو ویرایش کنید
# سپس برنامه خودکار تنظیمات رو می‌خواند
```

### 5️⃣ تست کردن

```bash
# اجرای تست‌ها
make test

# بنچمارک
make bench
```

---

## 📊 نمادهای محبوب

| نماد | توضیح |
|------|-------|
| `GC=F` | طلا (Futures) - پیش‌فرض |
| `EURUSD=X` | یورو/دلار |
| `GBPUSD=X` | پوند/دلار |
| `JPYUSD=X` | ین/دلار |
| `ES=F` | S&P 500 Futures |
| `NQ=F` | Nasdaq 100 Futures |
| `CL=F` | نفت خام |
| `NG=F` | گاز طبیعی |

## ⚙️ محدوده‌های زمانی

| محدوده | توضیح |
|--------|-------|
| `1d` | 1 روز |
| `5d` | 5 روز |
| `1mo` | 1 ماه |
| `3mo` | 3 ماه |
| `6mo` | 6 ماه |
| `1y` | 1 سال |
| `2y` | 2 سال |
| `5y` | 5 سال |

## 📈 بازه‌های زمانی

| بازه | توضیح |
|------|-------|
| `1m` | 1 دقیقه |
| `5m` | 5 دقیقه |
| `15m` | 15 دقیقه |
| `30m` | 30 دقیقه |
| `1h` | 1 ساعت - پیش‌فرض |
| `1d` | 1 روز |

## 🐳 Docker

```bash
# ساخت Docker image
docker build -t gold-analyzer .

# اجرای container
docker run -it gold-analyzer

# یا با docker-compose
docker-compose up -d

# مشاهده لاگ‌ها
docker-compose logs -f gold-analyzer
```

## 📁 ساختار فایل‌های مهم

```
gold-analyzer/
├── cmd/main.go              # برنامه اصلی
├── config/config.go         # تنظیمات
├── indicators/              # اندیکاتورهای تکنیکال
├── strategy/gold_strategy.go # منطق معاملاتی
├── yahoo/client.go          # کلاینت Yahoo Finance
├── Makefile                 # دستورات مفید
└── docker-compose.yml       # برای Docker
```

## 🔧 دستورات Makefile مفید

```bash
make help           # نمایش تمام دستورات
make build          # کامپایل
make run            # اجرا
make test           # تست‌ها
make bench          # بنچمارک
make fmt            # فرمت کد
make clean          # پاک کردن فایل‌های تولید شده
make check          # تمام بررسی‌ها (fmt + vet + test + lint)
make release        # ساخت نسخه release برای تمام سیستم‌عامل‌ها
```

## 💡 نکات مهم

### تغییر حد‌های سیگنال

```bash
RSI_BUY_LOWER=30 RSI_BUY_UPPER=70 RSI_SELL_THRESHOLD=80 go run ./cmd/main.go
```

### فاصله بررسی

تغییر فاصله بررسی (پیش‌فرض: 1 دقیقه):

```bash
CHECK_INTERVAL_MINUTES=5 go run ./cmd/main.go
```

### مشاهده لاگ‌های live

```bash
tail -f signals.log
```

## 🎯 سیگنال‌های ممکن

- ✅ **BUY** - خریدش کن
- ❌ **SELL** - بفروش
- ⏸️ **HOLD** - صبر کن

## ⚠️ توجهات مهم

1. این برنامه فقط تحلیل تکنیکال انجام می‌دهد
2. سیگنال‌ها تضمین شدهٔ نیستند
3. همیشه risk management رو در نظر بگیرید
4. قبل از معاملهٔ واقعی، روی حساب demo تست کنید

## 🆘 مشکلات رایج

### خطای "No data found, symbol may be delisted"
- نماد رو درست بررسی کنید
- از نمادهای محبوب استفاده کنید

### خطای Rate Limiting (429)
- برنامه خودکار retry می‌کند
- صبر کنید یا فاصله بررسی رو بیشتر کنید

### Logging کار نمی‌کند
- دایرکتوری لاگ را چک کنید
- اجازه نوشتن فایل رو بررسی کنید

## 📖 مطالعهٔ بیشتر

```bash
# مستندات کامل
cat README.md

# دیدن تمام تنظیمات
cat config/config.go

# تست‌ها
cat test/indicators_test.go
```

## 🚀 بعدی

1. ✅ برنامه رو اجرا کن
2. ✅ سیگنال‌ها رو مراقب کن
3. ✅ تنظیمات رو برحسب نیاز تغییر بده
4. ✅ روی live trading شروع کن (با احتیاط!)

---

**خوشبختی! حالا شما آماده هستید! 🎉**